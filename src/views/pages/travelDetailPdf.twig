<!DOCTYPE html>
<html lang="{{ app.language|default('fr') }}">
	<head>
		<meta charset="UTF-8">
		<title>{{ t('trip') }}
			:
			{{ voyage.name }}</title>
		<style>
			html,
			body {
				height: 100%;
				margin: 0;
				padding: 0;
				background: #f5faff;
				font-family: Arial, sans-serif;
				color: #222;
			}
			body {
				min-height: 100vh;
				padding: 48px 7vw;
				display: flex;
				justify-content: center;
				align-items: flex-start;
			}
			.pdf-container {
				max-width: 900px;
				width: 100%;
				background: #fff;
				padding: 20px 40px;
				border-radius: 0;
				box-sizing: border-box;
				display: flex;
				flex-direction: column;
				gap: 24px;
			}
			h1 {
				color: #007bff;
				text-align: center;
				font-size: 2.4em;
				margin-bottom: 16px;
				font-weight: 800;
			}
			.pdf-section {
				padding: 16px 24px;
				background: #f8f9fa;
				border-left: 5px solid #007bff;
				border-radius: 10px;
				box-shadow: 0 2px 8px rgba(0, 123, 255, 0.07);
			}
			.pdf-label {
				font-weight: bold;
				font-size: 1.1em;
				color: #007bff;
				margin-bottom: 8px;
				display: block;
			}
			.pdf-content {
				font-size: 1.05em;
				color: #333;
				line-height: 1.5;
				white-space: pre-wrap;
			}
			.photos-container {
				display: flex;
				gap: 10px;
				flex-wrap: wrap;
				justify-content: center;
				margin-bottom: 24px;
			}
			.photos-container img {
				max-height: 150px;
				border-radius: 10px;
				border: 1px solid #ddd;
			}
		</style>
	</head>
	<body>
		<div class="pdf-container">
			<h1>{{ voyage.name }}</h1>

			{% set photosArray = voyage.photos ? voyage.photos|split(';') : [] %}
			{% if photosArray|length > 0 %}
				<div class="photos-container">
					{% for photo in photosArray %}
<img src="{{ absoluteUrl }}/assets/img/{{ photo }}" alt="{{ voyage.name }} - photo {{ loop.index }}"/>
					{% endfor %}
				</div>
			{% endif %}

			<div class="pdf-section">
				<span class="pdf-label">{{ t('que_faire') }}
					:</span>
				<div class="pdf-content">{{ voyage.queFaire|nl2br }}</div>
			</div>

			<div class="pdf-section">
				<span class="pdf-label">{{ t('itinerary') }}
					:</span>
				<div class="pdf-content">{{ voyage.itinerary|nl2br }}</div>
			</div>

			<div class="pdf-section">
				<span class="pdf-label">{{ t('ou_dormir') }}
					:</span>
				<div class="pdf-content">{{ voyage.ouDormir|nl2br }}</div>
			</div>

			<div class="pdf-section">
				<span class="pdf-label">{{ t('ou_manger') }}
					:</span>
				<div class="pdf-content">{{ voyage.ouManger|nl2br }}</div>
			</div>

			<div class="pdf-section">
				<span class="pdf-label">{{ t('transports') }}
					:</span>
				<div class="pdf-content">{{ voyage.transports|nl2br }}</div>
			</div>
		</div>
	</body>
</html>
