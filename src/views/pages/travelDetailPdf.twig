<!DOCTYPE html>
<html lang="{{ app.language|default('fr') }}">
	<head>
		<meta charset="UTF-8">
		<title>{{ t('trip') }}
			:
			{{ voyage.name }}</title>
		<style>
			html,
body {
    height: 100%;
    margin: 0;
    padding: 0;
    background: #f5faff; /* bleu très clair */
    font-family: Arial, sans-serif;
    color: #222; /* texte foncé */
}

body {
    min-height: 100vh;
    padding: 48px 7vw;
    display: flex;
    justify-content: center;
    align-items: flex-start;
}

.pdf-container {
    max-width: 900px;
    width: 100%;
    max-width: 95vw;
    background: #fff;
    padding: 20px 40px;
    border-radius: 0;
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    gap: 24px;
}

h1 {
    color: #4caf50; /* vert doux et positif */
    text-align: center;
    font-size: 2.4em;
    margin-bottom: 16px;
    font-weight: 800;
}

.pdf-section {
    padding: 16px 24px;
    background: #e8f5e9; /* vert très clair pour fond */
    border-left: 5px solid #4caf50; /* bordure vert */
    border-radius: 10px;
    box-shadow: 0 2px 8px rgba(76, 175, 80, 0.07); /* ombre verte légère */
}

.pdf-label {
    font-weight: bold;
    font-size: 1.1em;
    color: #388e3c; /* vert plus foncé */
    margin-bottom: 8px;
    display: block;
}

.pdf-content {
    font-size: 1.05em;
    color: #222;
    line-height: 1.5;
    white-space: pre-wrap;
}

		</style>
	</head>
	<body>
		<div class="pdf-container">
			<h1>{{ voyage.name }}</h1>

			<div class="pdf-section">
				<span class="pdf-label">{{ t('que_faire') }}
					:</span>
				<div class="pdf-content">{{ voyage.queFaire|raw }}</div>
			</div>

			<div class="pdf-section">
				<span class="pdf-label">{{ t('itinerary') }}
					:</span>
				<div class="pdf-content">{{ voyage.itinerary|raw }}</div>
			</div>

			<div class="pdf-section">
				<span class="pdf-label">{{ t('ou_dormir') }}
					:</span>
				<div class="pdf-content">{{ voyage.ouDormir|raw }}</div>
			</div>

			<div class="pdf-section">
				<span class="pdf-label">{{ t('ou_manger') }}
					:</span>
				<div class="pdf-content">{{ voyage.ouManger|raw }}</div>
			</div>

			<div class="pdf-section">
				<span class="pdf-label">{{ t('transports') }}
					:</span>
				<div class="pdf-content">{{ voyage.transports|raw }}</div>
			</div>
		</div>
	</body>
</html>
