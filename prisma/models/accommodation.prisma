model Accommodation {
  id            Int           @id @default(autoincrement())
  title         String
  photo         String        // URL ou chemin vers une image
  priceCategory PriceCategory
  travel        Travel        @relation(fields: [travelId], references: [id])
  travelId      Int
}